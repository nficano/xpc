# pytest configuration for the agent test suite.
#
# These tests run on the host's modern Python (3.x), exercising the
# Python-3.4-compatible agent module. The agent module itself never imports
# anything outside the stdlib; tests are free to use modern features.
